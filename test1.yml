platform: linux

image_resource:
  type: docker-image
  source:
    repository: maven
    tag: latest

inputs:
  - name: project
outputs:
  - name: mvn-output

run:
  path: "mvn"
  args: ["-f", "project/jjhinit/pom.xml", "test"]
